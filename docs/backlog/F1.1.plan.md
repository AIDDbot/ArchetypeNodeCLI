# Implementation Plan

## Overview

Implement F1.1 “Initialize package and TypeScript config” by creating a minimal Node v24 + TypeScript scaffold with strict ESM configuration and a conventional `src/` layout. Keep dependencies minimal; add only what’s necessary to enable type checking and future growth. No CLI behavior yet.

References:
- Spec: /docs/backlog/F1.1.spec.md
- Design: /docs/backlog/F1.1.design.md

## Context

Instructions to follow during development:
- /.github/instructions/gid-architecture.instructions.md
- /.github/instructions/frm-node.instructions.md
- /.github/instructions/lng-typescript.instructions.md

## Tasks

- [ ] 1. Create minimal package.json
  - Define: name, version, private, type="module", license, engines { node ">=24" }
  - Add scripts: "typecheck": "tsc -p tsconfig.json --noEmit", "clean": node one-liner to remove dist/

- [ ] 2. Add TypeScript dev setup
  - Add devDependencies: typescript, @types/node
  - Pin to current stable versions (caret range)

- [ ] 3. Create strict ESM tsconfig.json
  - compilerOptions: { target: ES2022, module: ES2022, moduleResolution: Bundler, types: ["node"], strict: true, noEmit: true, skipLibCheck: true, forceConsistentCasingInFileNames: true, isolatedModules: true }
  - include: ["src/**/*.ts"], exclude: ["node_modules", "dist"]

- [ ] 4. Scaffold source layout
  - Create folder: src/
  - Add src/index.ts with a placeholder export (no runtime behavior)

- [ ] 5. Verify type-checking script (local)
  - Ensure `npm run typecheck` completes without errors on the empty scaffold

- [ ] 6. Keep scope minimal
  - Defer CLI entrypoint, Commander, Chalk, Zod, and runtime logic to later features (F1.2+)

> End of Feature Implementation Tasks for F1.1, last updated 2025-08-20.
