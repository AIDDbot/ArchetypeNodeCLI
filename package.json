{
  "name": "cursor-auditor",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "node scripts/build-hooks.mjs",
    "test": "node --test test/audit.test.mjs",
    "lint": "oxlint --fix --format=agent --quiet"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.2.0",
    "oxlint": "^1.80.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2"
  },
  "packageManager": "bun@1.4.0",
  "engines": {
    "node": ">=24.0.0"
  }
}